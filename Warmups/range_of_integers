"use strict"

//Write a Js program to get the integers in range(x,y)
// example: range(2,9) // var arr = [2,3,4,5,6,7,8]

function range (start, end) {
	var integers = [];
  for (var i = start; i < end; i++) {
    integers.push(i);
  }
  return integers;
}
console.log(range(2,9));
