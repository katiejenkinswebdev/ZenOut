// Write a function that takes two numbers and multiplies them without using *
//Stretch: write a curried version

// Write a function that takes two arrays and returns a new array with all items in alphabetical order.

function multiply (x, y) {
	var result = 0;
	for (var i =0; i < y; i++) {
	result += x;
}
return result;
}

multiply(2,5);

//takes 2 strings, combines them, and sorts them in alpha

arr1 = ["dog", "cat"];
arr2 = ["horse", "monkey"];

function sort(arr1, arr2){
return arr1.concat(arr2).sort();
//return [...arr1, ...arr2].sort();
//spread operator (takes all from array1 and lists out), rest operator (function(rest(firstNumber = first arg passed, ...myArgs = picks up the rest of the arguements))) ES6 features!
}

sort(arr1, arr2);
